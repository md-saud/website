import React from 'react';

function Header() {
  return (
    <header className="header">
      <h1>Saud's Portfolio</h1>
      <nav>
        <a href="#profile">Profile</a>
        <a href="#projects">Projects</a>
        <a href="#contact">Contact</a>
      </nav>
    </header>
  );
}

export default Header;
